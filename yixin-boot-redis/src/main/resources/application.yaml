spring:
  redis:
    host: 120.46.188.187
    timeout: 1800000  # 阅读超时毫秒
    lettuce:
      pool:
        max-active: 20  # 连接池最大连接数
        max-wait: -1   # 最大阻塞等待时间（负数表示没有限制）
        max-idle: 5    # 连接池中最大空闲连接
        min-idle: 0    # 连接池中最小空闲连接
    port: 6379
    database: 0